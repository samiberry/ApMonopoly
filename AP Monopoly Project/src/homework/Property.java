package homework;

public class Property extends AbstractProperty {

    public Property(String name, PropertyColor color, int price) {
        setName(name);
        setColor(color);
        setPrice(price);
    }
}
